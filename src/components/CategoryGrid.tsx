import Link from 'next/link'\nimport { categories } from '@/data/categories'\n\nexport default function CategoryGrid() {\n  return (\n    <div className=\"grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-4 md:gap-6\">\n      {categories.map(category => (\n        <Link key={category.id} href={`/category/${category.slug}`} className=\"group p-6 bg-white rounded-2xl border-2 border-gray-100 hover:border-sonic-red transition-all duration-300 shadow-premium hover:shadow-hover\">\n          <div className=\"text-center\">\n            <div className=\"text-4xl mb-3 group-hover:scale-125 transition-transform duration-300\">📋</div>\n            <h3 className=\"font-bold text-sm sm:text-base text-sonic-dark group-hover:text-sonic-red transition-colors line-clamp-2\">\n              {category.name}\n            </h3>\n            <p className=\"text-xs text-gray-500 mt-2 hidden md:block line-clamp-2\">\n              {category.description}\n            </p>\n          </div>\n        </Link>\n      ))}\n    </div>\n  )\n}\n